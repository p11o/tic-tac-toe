# tic-tac-toe

```bash
# trains model
> docker compose --profile train up

# run server to play/inference
# open localhost:8000
> docker compose --profile play up
```

To delete models
```bash
rm *.npy
```
